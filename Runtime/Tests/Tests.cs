using System;
using System.Collections.Generic;
using System.Text;
using Omni.Core;
using Omni.Core.Hashing;
using UnityEngine;
using static Omni.Core.Enums;

namespace Omni.Tests
{
    public class Tests : MonoBehaviour
    {
        private void Start()
        {
            // var IOhandler = new DataIOHandler(1024);
            // IOhandler.Write("Ruan");
            // byte[] iv = IOhandler.EncryptBuffer(new byte[16]);


            // IOhandler.DecryptBuffer(new byte[16], iv, 0);

            // OmniLogger.Print(IOhandler.ReadString());
        }
    }
}